[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[scripts]
dev = "uvicorn app.main:app --reload --port 5000"
start = "uvicorn app.main:app --host 0.0.0.0 --port 8000"
test = "pytest"

[packages]
fastapi = "*"
psycopg2 = "*"
typing-extensions = "*"
httpx = "*"
pytest = "*"

[requires]
python_version = "3.12"